version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD} 
      POSTGRES_USER: ${DB_USER}
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
    ports:
      - ${DB_PORT}:5432
    